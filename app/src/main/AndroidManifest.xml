<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiceRoom"
        android:usesCleartextTraffic="true"
        tools:targetApi="28">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.diceroom.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".authentication.SelectLoginActivity"
            android:exported="true" />
        <activity
            android:name=".authentication.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".authentication.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".authentication.ForgotPasswordActivity"
            android:exported="true" />
        <activity
            android:name=".authentication.ChangePasswordActivity"
            android:exported="true" />
        <activity
            android:name=".authentication.ProfileConfigActivity"
            android:exported="true" />
        <activity
            android:name=".MainMenuActivity"
            android:exported="true" />
        <activity
            android:name=".games.GameDetailsActivity"
            android:exported="true" />
    </application>

</manifest>